{
  "kind": "capability.request",
  "package": "com.example.plaza",
  "capabilities": [
    {
      "name": "webspace.input.pointer-lock",
      "required": false,
      "reason": "Mouse-look is smoother with pointer lock.",
      "durationSeconds": 3600,
      "fallback": "degrade"
    }
  ]
}
